.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
node_modules/
.DS_Store
.env
.env.local
.qoder/

# Claude Code 运行时自动生成 / 个人配置，不需要提交
.agents/
.claude/skills/
.claude/settings.local.json

# 部署临时配置（失败时也不得提交）
.env.new
.env.new.*
__pycache__/

# 本地直跑后端时的学生录音目录（docker 内在卷里）
backend/audio/

# ZCode 本地工具配置
.zcodeignore

# HTTPS 证书（acme.sh 签发到本地，不入库）
ssl/
